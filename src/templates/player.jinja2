<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Super fancy music player 2.0</title>
    <link rel="icon" href="/static/favicon.ico">
    <link rel="stylesheet" href="/style.css">
</head>
<body>

    <div id="bg-image-1" class="bg-image background-cover"></div>
    <div id="bg-image-2" class="bg-image background-cover fade-image-top"></div>

    <div class="sidebar sidebar-left">

        <div>

            <div class="box-rounded box-solid">

                <h3>{% trans %}Playlists{% endtrans %}</h3>

                <div id="playlist-checkboxes">
                    {% trans %}Loading...{% endtrans %}
                </div>
            </div>

            <div class="box-rounded box-solid">

                <h3>{% trans %}Playback{% endtrans %}</h3>

                <p>
                    <span id="current-track">{% trans %}Waiting for music in queue...{% endtrans %}</span>
                </p>
                <p class="secondary">
                    {% trans %}Previously: {% endtrans %} <span id="previous-track">-</span>
                </p>

                <div class="outer-bar">
                    <div id="progress-bar"></div>
                    <div class="flex-space-between">
                        <span id="progress-time-current" class="progress-time">--:--</span>
                        <span id="progress-time-duration" class="progress-time">--:--</span>
                    </div>
                </div>

                {% for button_name in ['skip-previous', 'rewind', 'play', 'pause', 'fast-forward', 'skip-next', 'microphone', 'album', 'gear'] %}
                    <button id="button-{{ button_name }}" class="icon-button">
                        <div class="icon" style="background-image: url('{{ assets[button_name + '.svg'] }}')"></div>
                    </button>
                {% endfor %}

                <input type="range" min="0" max="100" value="50" id="settings-volume">

            </div>

            <div class="box-rounded box-solid box-queue">
                <div class="flex-space-between">
                    <h3>{% trans %}Queue{% endtrans %} (<span id="current-queue-size">0</span>)</h3>
                    <button id="button-queue-add" class="icon-button">
                        <div class="icon" style="background-image: url('{{ assets['playlist-plus.svg'] }}')"></div>
                    </button>
                </div>
                <table id="queue-table"></table>
            </div>

        </div>

    </div>

    <div class="sidebar sidebar-right" id="sidebar-album-covers">
        <div class="album-covers">
            <div id="album-cover-1" class="background-cover box-rounded"></div>
            <div id="album-cover-2" class="background-cover box-rounded fade-image-top"></div>
        </div>
    </div>

    <div class="sidebar sidebar-right" id="sidebar-lyrics">
        <div class="lyrics-outer box-rounded box-solid">
            <div id="lyrics"></div>
        </div>
    </div>

    <div id="audio"></div>

    <div id="queue-overlay" class="full-page-overlay">
        <div class="box-rounded box-solid">
            <div class="flex-space-between">
                <h3>{% trans %}Add track to queue{% endtrans %}</h3>
                <span>
                    <button id="queue-close" class="icon-button">
                        <div class="icon" style="background-image: url('{{ assets['close.svg'] }}')"></div>
                    </button>
                </span>
            </div>

            <select id="track-list-playlist">
                <option value="everyone">{% trans %}Everyone{% endtrans %}</option>
                {% for playlist in main_playlists %}
                    <option value="{{ playlist.dir_name }}">{{ playlist.display_name }}</option>
                {% endfor %}
                {% for playlist in guest_playlists %}
                    <option value="{{ playlist.dir_name }}">{{ playlist.display_name }} ({% trans %}Guest{% endtrans%})</option>
                {% endfor %}
            </select>

            <input id="track-list-query" type="text" placeholder="{% trans %}Search query{% endtrans %}">

            <div id="track-list-output">

            </div>
        </div>
    </div>

    <div id="settings-overlay" class="full-page-overlay">
        <div class="box-rounded box-solid">
            <div class="flex-space-between">
                <h3>{% trans %}Hotkeys{% endtrans %}</h3>
                <span>
                    <button id="settings-close" class="icon-button">
                        <div class="icon" style="background-image: url('{{ assets['close.svg'] }}')"></div>
                    </button>
                </span>
            </div>
            <table class="hotkeys-table">
                <tbody>
                    <tr>
                        <td><kbd>1</kbd> - <kbd>9</kbd></td>
                        <td>{% trans %}Toggle playlists{% endtrans %}</td>
                    </tr>
                    <tr>
                        <td><kbd>←</kbd></td>
                        <td>{% trans %}Previous track{% endtrans %}</td>
                    </tr>
                    <tr>
                        <td><kbd>→</kbd></td>
                        <td>{% trans %}Next track{% endtrans %}</td>
                    </tr>
                    <tr>
                        <td><kbd>&lt;</kbd> / <kbd>&gt;</kbd>
                        <td>{% trans %}Seek in track (don't use shift){% endtrans %}</td>
                    </tr>
                </tbody>
            </table>

            <h3>{% trans %}Settings{% endtrans %}</h3>
            <div>
                <input type="number" value="4" style="width: 3rem;" id="settings-queue-size">
                <label for="settings-queue-size">{% trans %}Minimum queue length{% endtrans %}</label>
            </div>
            <div>
                <select id="settings-audio-quality">
                    <option value="verylow">{% trans %}Potato (~15MB per hour){% endtrans %}</option>
                    <option value="low">{% trans %}Low (~35MB per hour){% endtrans %}</option>
                    <option value="high" selected>{% trans %}High (~50MB per hour){% endtrans %}</option>
                </select>
                <label for="settings-audio-quality">{% trans %}Audio quality{% endtrans %}</label>
            </div>
            <div>
                <select id="settings-queue-removal-behaviour">
                    <option value="same" selected>{% trans %}New track from same playlist{% endtrans %}</option>
                    <option value="roundrobin">{% trans %}New track from next round-robin playlist{% endtrans %}</option>
                </select>
                <label for="settings-queue-removal-behaviour">{% trans %}Queue removal behaviour{% endtrans %}</label>
            </div>

            <h3>{% trans %}YouTube download{% endtrans %}</h3>
            {% trans %}Select a name and paste a link from YouTube, Soundcloud, Bandcamp, etc. Finally, press <kbd>Download</kbd>.{% endtrans %}
            <form>
                <select id="youtube-dl-directory">
                    {% for playlist in main_playlists %}
                        <option value="{{ playlist.dir_name }}">{{ playlist.display_name }}</option>
                    {% endfor %}
                    {% for playlist in guest_playlists %}
                        <option value="{{ playlist.dir_name }}">{{ playlist.display_name }} (Gast)</option>
                    {% endfor %}
                </select>
                <input id="youtube-dl-url" type="text" placeholder="https://youtube.com/watch?v=..." size="45">
                <input id="youtube-dl-submit" type="submit" value="{% trans %}Download{% endtrans %}">
                <div id="youtube-dl-spinner" style="visibility: hidden" class="spinner"></div>
                <br>
                <div id="youtube-dl-output">{% trans %}yt-dlp output will appear here{% endtrans %}</div>
            </form>

            <h3>{% trans %}Credits{% endtrans %}</h3>
            {% trans trimmed gh="https://github.com/DanielKoomen/WebApp", robin_gh="https://github.com/Derkades", insp_gh="https://github.com/DanielKoomen/MusicPlayer", daniel_gh="https://github.com/DanielKoomen" %}
            <a href="{{gh}}">Super fancy music player 2.0</a> is a project largely made by
            <a href="{{robin_gh}}">Robin Slot</a>, inspired by the awesome
            <a href="{{insp_gh}}" target="_blank">Super fancy music player</a> by
            <a href="{{daniel_gh}}" target="_blank">Daniël Koomen</a>. Enjoy!
            {% endtrans %}
        </div>
    </div>

    <div id="loading-overlay" class="full-page-overlay">
        <div>
            <div class="spinner spinner-large"></div>
            <div>
                <h3 class="loading-text">{% trans %}Loading...{% endtrans %}</h3>
            </div>
        </div>
    </div>

    <div class="vignette"></div>

    {# A "variable" to be used by javascript #}
    <span id="delete-base64" style="display: none">{{ assets['delete.svg'] }}</span>

    <script src="/static/script.js"></script>

</body>
</html>
