<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Super fancy music player 2.0</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>

    <div id="bg-image-1" class="bg-image background-cover"></div>
    <div id="bg-image-2" class="bg-image background-cover fade-image-top"></div>

    <div class="sidebar sidebar-left">

        <div>

            <div class="box-rounded box-solid">

                <h3>Afspeellijsten</h3>

                {% for person in raphson %}
                    <span class="checkbox-with-label">
                        <input type="checkbox" class="person-checkbox" id="checkbox-{{ person.dir_name }}" checked>
                        <label for="checkbox-{{ person.dir_name }}">{{ person.display_name }}<sup>{{ loop.index }}</sup> ({{ person.count_tracks() }})</label>
                    </span>
                {% endfor %}

                {% if guests %}
                    <hr>
                    {% for person in guests %}
                        <span class="checkbox-with-label">
                            <input type="checkbox" class="person-checkbox" id="checkbox-{{ person.dir_name }}">
                            <label for="checkbox-{{ person.dir_name }}">{{ person.display_name }}<sup>{{ loop.index + 3}}</sup> ({{ person.count_tracks() }})</label>
                        </span>
                    {% endfor %}
                {% endif %}

            </div>

            <div class="box-rounded box-solid">

                <h3>Muziek</h3>

                <p>
                    <span id="current-track">Wachten op muziek in wachtrij...</span>
                </p>
                <p class="secondary">
                    Vorige: <span id="previous-track">-</span>
                </p>

                <div class="outer-bar">
                    <div id="progress-bar"></div>
                    <div class="flex-space-between">
                        <span id="progress-time-current">--:--</span>
                        <span id="progress-time-duration">--:--</span>
                    </div>
                </div>

                {% for button_name in ['backward-fast', 'backward', 'play', 'pause', 'forward', 'forward-fast', 'forward-step', 'closed-captioning', 'record-vinyl', 'square-plus'] %}
                    <button id="button-{{ button_name }}" class="icon-button">
                        <div class="icon" style="background-image: url('{{ assets[button_name + '.svg'] }}')"></div>
                    </button>
                {% endfor %}


            </div>

            <div class="box-rounded box-solid box-queue">
                <h3>Wachtrij</h3>
                <div id="queue"></div>
            </div>

        </div>

    </div>

    <div class="sidebar sidebar-right" id="sidebar-album-covers">
        <div class="album-covers">
            <div id="album-cover-1" class="background-cover box-rounded"></div>
            <div id="album-cover-2" class="background-cover box-rounded fade-image-top"></div>
        </div>
    </div>

    <div class="sidebar sidebar-right" id="sidebar-lyrics">
        <div class="lyrics-outer box-rounded box-solid">
            <div id="lyrics"></div>
        </div>
    </div>

    <div id="audio"></div>

    <button class="icon-button" id="settings-open">
        <div class="icon" style="background-image: url('{{ assets['gear.svg'] }}')">
    </button>

    <div id="queue-overlay" class="full-page-overlay">
        <div class="box-rounded box-solid">
            <div class="flex-space-between">
                <h3>Nieuw nummer aan wachtrij toevoegen (experimenteel)</h3>
                <button id="queue-close">Sluiten</button>
            </div>

            <ol>
                <li>Vul een korte zoekopdracht in en klik op 'Zoeken' (kan lang duren, heb geduld en klik niet meerdere keren!)</li>
                <li>Selecteer een knop uit de zoekresultaten</li>
                <li>Even wachten, het nummer wordt in de achtergrond gedownload en toegevoegd aan de wachtrij</li>
            </ol>

            <br>

            <input id="queue-search-query" type="text" placeholder="zoekopdracht">
            <button id="queue-search-perform">Zoeken</button>
            <br>
            <div id="queue-search-results">
            </div>
        </div>
    </div>

    <div id="settings-overlay" class="full-page-overlay">
        <div class="box-rounded box-solid">
            <div class="flex-space-between">
                <h3>Hotkeys</h3>
                <button id="settings-close">Sluiten</button>
            </div>

            <table class="hotkeys-table">
                <tbody>
                    <tr>
                        <td><kbd>1</kbd> - <kbd>9</kbd></td>
                        <td>checkboxes van personen</td>
                    </tr>
                    <tr>
                        <td><kbd>ArrowRight</kbd> / <kbd>f</kbd></td>
                        <td>volgend nummer</td>
                    </tr>
                    <tr>
                        <td><kbd>&lt;</kbd> / <kbd>&gt;</kbd>
                        <td>seek (geen shift indrukken)</td>
                    </tr>
                </tbody>
            </table>

            <h3>YouTube download</h3>
            Selecteer een naam, plak een link van YouTube, Soundcloud, Bandcamp, etc. en druk op <kbd>Download</kbd>.
            <form>
                <select id="youtube-dl-directory">
                    {% for person in raphson %}
                        <option value="{{ person.dir_name }}">{{ person.display_name }}</option>
                    {% endfor %}
                    {% for person in guests %}
                        <option value="{{ person.dir_name }}">{{ person.display_name }} (Gast)</option>
                    {% endfor %}
                </select>
                <input id="youtube-dl-url" placeholder="https://youtube.com/watch?v=...">
                <input id="youtube-dl-submit" type="submit" value="Download">
                <div id="youtube-dl-spinner" style="visibility: hidden" class="spinner"></div>
                <br>
                <div id="youtube-dl-output">Output komt hier</div>
            </form>

            <h3>Credits</h3>
            <a href="https://github.com/DanielKoomen/WebApp">Super fancy music player 2.0</a> is een
            project (grotendeels) gemaakt door <a href="https://github.com/Derkades">Robin Slot</a>,
            geïnspireerd door de geweldige <a href="https://github.com/DanielKoomen/MusicPlayer" target="_blank">Super fancy music player</a>
            van <a href="https://github.com/DanielKoomen" target="_blank">Daniël Koomen</a>. Veel luisterplezier!
        </div>
    </div>

    <div class="vignette"></div>

    {# A "variable" to be used by javascript #}
    <span id="trash-can-base64" style="display: none">{{ assets['trash-can.svg'] }}</span>

    <script src="/script.js"></script>

</body>
</html>
