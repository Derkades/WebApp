<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Super fancy music player 2.0</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>

    <div id="bg-image-1" class="bg-image background-cover"></div>
    <div id="bg-image-2" class="bg-image background-cover fade-image-top"></div>

    <div id="left-sidebar" class="sidebar">

        <div>

            <div class="box-rounded box-controls">

                <h3>Raphson</h3>

                <span id="main-checkboxes">
                    {% for person in raphson %}
                        <input type="checkbox" class="person-checkbox" id="checkbox-{{ person.dir_name }}" checked>
                        <label for="checkbox-{{ person.dir_name }}">{{ person.display_name }}<sup>{{ loop.index }}</sup> ({{ person.count_tracks() }})</label>
                    {% endfor %}
                </span>

                <br>

                <h3>Gasten</h3>

                {% if guests %}
                    {% for person in guests %}
                        <input type="checkbox" class="person-checkbox" id="checkbox-{{ person.dir_name }}" checked>
                        <label for="checkbox-{{ person.dir_name }}">{{ person.display_name }}<sup>{{ loop.index + 3}}</sup> ({{ person.count_tracks() }})</label>
                    {% endfor %}
                {% else %}
                    <p>Er zijn geen mappen voor gasten</p>
                {% endif %}

                <br>

            </div>

            <div class="box-rounded box-controls">

                <h3>Muziek</h3>

                <p>
                    <span id="current-track">Downloaden...</span>
                </p>
                <p class="secondary">
                    Vorige: <span id="previous-track">-</span>
                </p>

                <div class="outer-bar">
                    <div id="progress-bar"></div>
                </div>

                {% for button_name in ['backward-fast', 'backward', 'play', 'pause', 'forward', 'forward-fast', 'forward-step'] %}
                    <button id="button-{{ button_name }}" class="icon-button">
                        <div class="icon" style="background-image: url('{{ assets[button_name + '.svg'] }}')">
                    </button>
                {% endfor %}

                <span id="progress-time"></span>
            </div>

            <div class="box-rounded box-controls">
                <h3>Wachtrij</h3>
                <div id="queue"></div>
            </div>

        </div>

    </div>

    <div id="right-sidebar" class="sidebar">
        <div class="album-covers">
            <div id="album-cover-1" class="background-cover box-rounded"></div>
            <div id="album-cover-2" class="background-cover box-rounded fade-image-top"></div>
        </div>
    </div>

    <div id="audio"></div>

    <button class="icon-button" id="settings-open">
        <div class="icon" style="background-image: url('{{ assets['gear.svg'] }}')">
    </button>

    <div id="settings-overlay">
        <div class="box-rounded box-controls">
            {# Veel te veel moeite om de 'Sluiten' knop op dezelde regel als de <h3> te krijgen... #}
            <div style="display: flex; justify-content: space-between; width: 100%">
                <h3 style="display: inline">Hotkeys</h3>
                <button id="settings-close">Sluiten</button>
            </div>

            <table class="hotkeys-table">
                <tbody>
                    <tr>
                        <td><kbd>1</kbd> - <kbd>9</kbd></td>
                        <td>checkboxes van personen</td>
                    </tr>
                    <tr>
                        <td><kbd>ArrowRight</kbd> / <kbd>f</kbd></td>
                        <td>volgend nummer</td>
                    </tr>
                    <tr>
                        <td><kbd>&lt;</kbd> / <kbd>&gt;</kbd>
                        <td>seek (geen shift indrukken)</td>
                    </tr>
                </tbody>
            </table>

            <h3>YouTube download</h3>
            Selecteer een naam, plak een link van YouTube, Soundcloud, Bandcamp, etc. en druk op <kbd>Download</kbd>.
            <form>
                <select id="youtube-dl-directory">
                    {% for person in raphson %}
                        <option value="{{ person.dir_name }}">{{ person.display_name }}</option>
                    {% endfor %}
                    {% for person in guests %}
                        <option value="{{ person.dir_name }}">{{ person.display_name }} (Gast)</option>
                    {% endfor %}
                </select>
                <input id="youtube-dl-url" placeholder="https://youtube.com/watch?v=...">
                <input id="youtube-dl-submit" type="submit" value="Download">
                <div id="youtube-dl-spinner" style="visibility: hidden" class="spinner"></div>
                <br>
                <div id="youtube-dl-output">Output will show here</div>
            </form>
        </div>
    </div>

    {# A "variable" to be used by javascript #}
    <span id="trash-can-base64" style="display: none">{{ assets['trash-can.svg'] }}</span>

    <script src="/script.js"></script>

</body>
</html>
